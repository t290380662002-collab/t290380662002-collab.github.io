services:
  - type: worker
    name: angetv10-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python tg_bot.py
    envVars:
      - key: TG_TOKEN
        sync: false
      - key: RENDER
        value: "1"
